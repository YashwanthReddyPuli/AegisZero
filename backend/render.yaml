services:
  - type: web
    name: aegis-api-backend
    env: python
    buildCommand: pip install -r requirements.txt && python train_threat_model.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
